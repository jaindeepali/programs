#include <iostream>
#include <cstdio>

using namespace std;

int main()
{
    int i,n,j,t,k;
    string s,o;
    scanf("%d", &n);
    i=0;
    while(n!=0)
    {
        scanf("%s", &s);
        t=s.length();

        printf("%s\n", &o);
        scanf("%d", &n);
    }

    return 0;
}
