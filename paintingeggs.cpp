//ac
#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    int t;
    double a,g,sa=0,sg=0;
    scanf("%d", &t);
    while(t--)
    {
        cin>>a>>g;
        if(abs(sa-sg)>500)
        {
                cout<<"-1";
                break;
        }
        else if
        {
            sa += a;

        }
        else if(abs(sa-sg)>500)
            sa -= a;
            sg += a;
    }
return 0;
}
